#pragma once

namespace Eclipt
{
    namespace Enums
    {
        enum ShaderType
        {
            fragmentShader = 0,
            vertexShader = 1
        };

        enum BorderType 
        {
            none = 0,
            fill = 1,
            solid = 2
        };
    }
}